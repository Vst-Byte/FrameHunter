<Page x:Class="FrameHunterFPS.SystemTweaksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="950" d:DesignWidth="1150"
      Title="SystemTweaksPage"
      FontFamily="Rajdhani, Bahnschrift, Segoe UI">

    <Page.Resources>
        <SolidColorBrush x:Key="BrushCyan" Color="#00FFFF"/>
        <SolidColorBrush x:Key="BrushPurple" Color="#BC13FE"/>

        <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#00FFFF" Offset="0"/>
            <GradientStop Color="#BC13FE" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Border x:Name="Track" Width="42" Height="22" CornerRadius="11" BorderThickness="1" BorderBrush="#333" Background="#111"/>
                            <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="#888" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource BrandGradient}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OptionCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="MinHeight" Value="80"/>
            <Setter Property="Background" Value="#1F1F23"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <EventSetter Event="MouseLeftButtonDown" Handler="Card_MouseLeftButtonDown"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#BC13FE"/>
                    <Setter Property="Background" Value="#25252B"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#BC13FE" BlurRadius="15" Opacity="0.3" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MinimalBtn" TargetType="Button">
            <Setter Property="Background" Value="#111"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="22" BorderThickness="1" BorderBrush="#444" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#BC13FE"/>
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MinimalActionBtn" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="actionBorder" CornerRadius="22" Background="{StaticResource BrandGradient}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="actionBorder" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="actionBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#BC13FE" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="DescText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Page.Resources>

    <Grid Background="#141417">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,40,0,30" HorizontalAlignment="Center">
            <TextBlock Text="SYSTEM OPTIMIZATION" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource BrandGradient}"/>
            <TextBlock Text="Tweaks organized for maximum performance and stability." Foreground="#777" FontSize="14" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
            <Grid MaxWidth="1100" HorizontalAlignment="Center" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="CORE PERFORMANCE" Foreground="{StaticResource BrushCyan}" FontWeight="Bold" Margin="5,0,0,10" FontSize="12"/>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Ultimate Power Plan" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Forces max CPU clocks." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbPower" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Unpark CPU Cores" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Prevents cores from sleeping." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbCpu" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable Fast Startup" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Ensures clean boot state." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbFastStartup" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable Hibernation" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Frees SSD space." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbHibernation" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable USB Suspend" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Prevents device sleep lag." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbUSBPower" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="GAMING &amp; LATENCY" Foreground="{StaticResource BrushPurple}" FontWeight="Bold" Margin="5,0,0,10" FontSize="12"/>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Windows Game Mode" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Prioritizes gaming threads." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbGameMode" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Hardware GPU Sched." Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Reduces GPU load." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbHAGS" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable VBS / HVCI" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Huge boost for Win 11." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbVBS" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Timer Resolution" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Forces 0.5ms latency." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbTimer" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Mouse Accel Fix" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="1:1 raw input movement." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbMouse" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="SYSTEM DEBLOAT" Foreground="White" FontWeight="Bold" Margin="5,0,0,10" FontSize="12"/>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Remove Bloatware" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Stops Edge background apps." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbBloat" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable Telemetry" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Stops data collection." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbTelemetry" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Visual Performance" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Disables UI animations." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbVisualFX" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable Transparency" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Lowers DWM GPU usage." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbTransparency" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource OptionCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Disable Xbox Game Bar" Style="{StaticResource TitleText}"/>
                                <TextBlock Text="Removes overlays." Style="{StaticResource DescText}"/>
                            </StackPanel>
                            <CheckBox x:Name="cbGameBar" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"/>
                        </Grid>
                    </Border>
                    <CheckBox x:Name="cbFSO" Visibility="Collapsed"/>
                    <CheckBox x:Name="cbWinOpt" Visibility="Collapsed"/>
                    <CheckBox x:Name="cbWidgets" Visibility="Collapsed"/>
                    <CheckBox x:Name="cbSticky" Visibility="Collapsed"/>
                    <CheckBox x:Name="cbErrorReporting" Visibility="Collapsed"/>
                    <CheckBox x:Name="cbIndexing" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="BtnRestore" Content="RESTORE STOCK" Width="180" Margin="0,0,20,0" Style="{StaticResource MinimalBtn}" Click="BtnRestore_Click"/>
                <Button x:Name="BtnSelectAll" Content="SELECT ALL" Width="180" Margin="0,0,20,0" Style="{StaticResource MinimalBtn}" Click="BtnSelectAll_Click"/>
                <Button Content="APPLY OPTIMIZATIONS" Width="240" Style="{StaticResource MinimalActionBtn}" Click="BtnApply_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>